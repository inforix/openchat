{
  "name": "@openchat/relay",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "tsx watch src/cli.ts",
    "start": "tsx src/cli.ts",
    "build": "echo \"No build yet\"",
    "vitest": "vitest",
    "test": "vitest run",
    "lint": "tsc --project tsconfig.json --noEmit",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@openchat/store": "workspace:*",
    "fastify": "^5.2.1",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.3",
    "vitest": "^2.1.9"
  }
}
